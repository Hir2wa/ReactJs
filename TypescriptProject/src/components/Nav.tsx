import React from "react";
type propsType = {
  viewCart: boolean;
  setViewCart: React.Dispatch<React.SetStateAction<boolean>>;
};
const Nav = () => {
  return <div>Nav</div>;
};

export default Nav;
